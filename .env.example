# Backend toggles (default: all simulated)
# Copy to .env and adjust as needed

# Use real IPFS via ipfshttpclient
USE_REAL_IPFS=0
# IPFS HTTP/multiaddr API endpoint (default localhost)
# You can set either IPFS_API_ADDR (multiaddr) or IPFS_API_URL (http URL).
# IPFS_API_ADDR=/ip4/127.0.0.1/tcp/5001/http
# IPFS_API_URL=http://127.0.0.1:5001
# Optional gateway base for building public links
# IPFS_GATEWAY_URL=http://127.0.0.1:8080/ipfs

# Optional AES-GCM encryption key for datasets (base64, 16/24/32 bytes)
# Example (32 bytes AES-256 key):
# IPFS_ENC_KEY=3q2+7wAAAAAAAAAAAAAAAAAAAAAAAACf39/9fQ==

# Use real EVM backend via web3.py
USE_REAL_EVM=0
WEB3_PROVIDER_URI=http://127.0.0.1:8545
EVM_CHAIN_ID=31337
EVM_PRIVATE_KEY=
MEDICAL_CONTRACT_ADDRESS=
VERIFIER_CONTRACT_ADDRESS=

# Use real SNARKs via circom/snarkjs
USE_REAL_SNARK=0
